{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Eureka desktop window.",
  "windows": ["main", "startup-error"],
  "permissions": ["core:default", "allow-open-external-url"]
}
