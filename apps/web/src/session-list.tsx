import type { WebSessionSummary } from "@pi-web/protocol";
export function SessionList({ sessions, currentId, disabled, onNew, onSwitch }: { sessions: WebSessionSummary[]; currentId?: string; disabled: boolean; onNew: () => void; onSwitch: (id: string) => void }) { return <section><h2>SESSIONS</h2><button disabled={disabled} onClick={onNew}>+ New Session</button>{sessions.map((session) => <button key={session.id} disabled={disabled || session.id === currentId} className={session.id === currentId ? "active" : ""} onClick={() => onSwitch(session.id)}>{session.name || session.preview || "New session"}</button>)}</section>; }
